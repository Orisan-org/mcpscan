# Corpus labels

Real corpus labels are intentionally not committed yet.

Add one `*.yaml` file per server only after the corpus selection criteria have
selected the server and a reviewer has written property-level labels. Do not
write labels for real servers during harness or scorer development.
